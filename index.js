/**
 * @format
 */

import {AppRegistry} from 'react-native';
import RootApp from './App/Screens/Welcome';
import {name as appName} from './App/app.json';

AppRegistry.registerComponent(appName, () => RootApp);
